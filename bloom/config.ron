(
    view: (
        bpm: 80,
        width: 640,
        height: 480,
        target_fps: 60,
        dynamic: true,
        vsync: true,
        screenshot: false,
        screenshot_path: "output/",
        locked_speed: false,
        fullscreen: false,
    ),
    server: (
        ip: "127.0.0.1",
        port: 3000,
        enable: false,
    ),
    inputs: {
        "InputImage": (
            type: "Video",
            path: "res/butterfly.mp4",
            width: 1366,
            height: 768,
            speed: (Fps: 23.98),
        ),
    },
    filters: {
        "blur": (
            inputs: [
                "iChannel0",
                "dither",
            ],
            vertex_shader: [
                "#std/default.vs.glsl",
            ],
            fragment_shader: [
                "#std/header.glsl",
                "#std/noise.glsl",
                "src/blur_fast.fs.glsl",
            ],
            variables: {
                "dither": Bool(false),
            },
        ),
        "threshold": (
            inputs: [
                "iChannel0",
            ],
            vertex_shader: [
                "#std/default.vs.glsl",
            ],
            fragment_shader: [
                "#std/header.glsl",
                "src/threshold.fs.glsl",
            ],
            variables: {},
        ),
        "image": (
            inputs: [
                "iChannel0",
                "iChannel1",
                "iChannel2",
            ],
            vertex_shader: [
                "#std/default.vs.glsl",
            ],
            fragment_shader: [
                "#std/header.glsl",
                "#std/noise.glsl",
                "src/target.fs.glsl",
            ],
            variables: {},
        ),
    },
    render_chain: [
        (
            name: "Threshold",
            filter: "threshold",
            inputs: {
                "iChannel0": Linear("InputImage"),
            },
            variables: {},
            precision: U8,
        ),
        (
            name: "Blur1",
            filter: "blur",
            inputs: {
                "iChannel0": Mipmaps("Threshold"),
            },
            variables: {
                "dither": Bool(true),
            },
            precision: U8,
        ),
        ( 
            name: "Blur2",
            filter: "blur",
            inputs: {
                "iChannel0": Mipmaps("Blur1"),
            },
            variables: {
                "dither": Bool(true),
            },
            precision: U8,
        ),
        ( 
            name: "Blur3",
            filter: "blur",
            inputs: {
                "iChannel0": Mipmaps("Blur2"),
            },
            variables: {
                "dither": Bool(true),
            },
            precision: U8,
        ),

    ],
    final_stage: (
        name: "Image",
        filter: "image",
        inputs: {
            "iChannel0": Linear("InputImage"),
            "iChannel1": Linear("Blur3"),
        },
        variables: {},
        precision: U8,
        mipmapped: false,
    ),
)
